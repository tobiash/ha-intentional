[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ha-intentional"
version = "0.7.13"
description = "Declarative intent-based automation for Home Assistant"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
authors = [
    { name = "tobiash", email = "tobiash@users.noreply.github.com" }
]
keywords = ["home-assistant", "home-automation", "hacs", "intent", "automation"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: End Users/Desktop",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Home Automation",
]
dependencies = [
    "Jinja2>=3.1",
    "PyYAML>=6.0",
    "voluptuous>=0.13.1",
]

[project.optional-dependencies]
test = [
    "pytest>=7.4",
    "pytest-cov>=4.1",
    "pytest-asyncio>=0.23",
    "aiohttp>=3.9",
]
# pytest-homeassistant-custom-component pins homeassistant strictly,
# so don't pin it here. The extra installs whatever HA version the
# test harness requires.
integration = [
    "pytest-homeassistant-custom-component>=0.13.250",
]
dev = [
    "ruff>=0.3",
    "mypy>=1.8",
    "pre-commit>=3.6",
]
# All test dependencies, including the heavy integration ones.
# Install with: pip install -e ".[all-tests]"
all-tests = [
    "ha-intentional[test]",
    "ha-intentional[integration]",
    "ha-intentional[dev]",
]

[project.urls]
Homepage = "https://github.com/tobiash/ha-intentional"
Repository = "https://github.com/tobiash/ha-intentional.git"
Issues = "https://github.com/tobiash/ha-intentional/issues"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
intentional = ["py.typed"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
asyncio_mode = "auto"
addopts = [
    "-ra",
    "--strict-markers",
    "--tb=short",
]
markers = [
    "e2e_config_flow: end-to-end config flow tests that need to run in a separate pytest invocation (see test_e2e_config_flow.py docstring for the pytest-homeassistant-custom-component translation cache pollution issue).",
]

[tool.coverage.run]
source = ["src/intentional"]
omit = ["*/tests/*", "*/__init__.py"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "if TYPE_CHECKING:",
]

[tool.ruff]
line-length = 100
target-version = "py311"
extend-exclude = ["custom_components", "tests/fixtures"]

[tool.ruff.lint]
select = [
    "E", "F", "W", "I", "N", "UP", "B", "C4", "SIM",
]
ignore = [
    "E501",  # line too long (handled by formatter)
    "B008",  # function call in default argument (used in voluptuous schemas)
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["S101", "B011"]
"custom_components/*" = ["F401", "E402"]   # F401: HA conventions allow re-exports
"custom_components/intentional/__init__.py" = ["F401"]

[tool.mypy]
python_version = "3.11"
ignore_errors = true   # we use dynamic types by design; types are a future improvement
follow_imports = "silent"
ignore_missing_imports = true
